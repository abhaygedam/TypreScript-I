


let nam: string = "abhay";

let age: number = 22;

let isFetching: boolean = false;

let ar: Array<number> = [1, 2, 3, 4, 5];

let ar2: string[] = ["a", "b", "c", "d", "e", "f"];

let tulip: [string, boolean] = ["abhay", true];

enum data {
    user,
    SuperUser,
    Admin,
    SuperAdmin,
}

function Product(x: number, y: number):number {
   return x * y;
}

function Divide(x: number, y: number):number {
    return x / y;
}

function Print(name:string):void {
    console.log(name);
}

